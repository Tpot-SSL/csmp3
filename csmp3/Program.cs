using System;
using System.IO;
namespace csmp3 {
    class Program {
        static void Main(string[] args) {
            //using(FileStream f2 = new FileStream("output.raw", FileMode.Create)) {
            //    using(FileStream f = new FileStream("test.mp3", FileMode.Open)) {
            //        Console.WriteLine(new Sound(f).Decode(f2));
            //        Console.ReadLine();
            //    }
            //}
        }
    }
}
